// Default API URL — overridden at runtime via Settings screen (saved to device storage)
// You don't need to change this file; just open the app → Settings → Server URL → type your IP
export const DEFAULT_API_URL = 'http://192.168.0.100:3001';
export const POLL_INTERVAL = 30000; // 30s refresh
